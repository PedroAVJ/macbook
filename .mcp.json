{
  "mcpServers": {
    "macbook-credential-broker": {
      "type": "stdio",
      "command": "/bin/sh",
      "args": [
        "-c",
        "root=$CLAUDE_PLUGIN_ROOT; if [ -z \"$root\" ]; then root=.; fi; exec \"$root/scripts/launch-credential-broker-mcp\""
      ],
      "cwd": "."
    }
  }
}
